export class NewAuthorizedPerson {

    name:string;
    images: string[];

    constructor(name: string, images: string[]) {

        this.name = name;
        this.images = images;
        
    }

}